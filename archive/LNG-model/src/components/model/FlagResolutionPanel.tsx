import { BASE_ASSUMPTIONS, P1_FLAG_IDS, P2_FLAG_IDS } from '@/lib/model/assumptions';
const group = (ids: string[], label: string) => <details className="rounded-lg border bg-white p-4" open><summary className="cursor-pointer font-semibold text-[#003366]">{label}</summary><div className="mt-3 space-y-3">{ids.map((id) => <div className="rounded bg-slate-50 p-3 text-sm" key={id}><strong>{id}</strong> — {BASE_ASSUMPTIONS[id]?.label}<br /><span>Default: {String(BASE_ASSUMPTIONS[id]?.flagDefault)}. Basis: {BASE_ASSUMPTIONS[id]?.flagDefaultBasis}</span><br /><span className="text-xs text-slate-500">Resolution path: replace flag default in Module 1 with ministry-approved actual or documented proxy.</span></div>)}</div></details>;
export default function FlagResolutionPanel() { const allFlags = Object.keys(BASE_ASSUMPTIONS).filter((id) => BASE_ASSUMPTIONS[id].classification === 'FLAG'); const p3 = allFlags.filter((id) => !P1_FLAG_IDS.includes(id) && !P2_FLAG_IDS.includes(id)); return <div className="space-y-3">{group(P1_FLAG_IDS, 'P1 — most consequential')}{group(P2_FLAG_IDS, 'P2 — material')}{group(p3, 'P3 — other flags')}</div>; }
