import { NavLink } from 'react-router-dom';
import { ACTUAL_COUNT, FLAG_COUNT } from '@/lib/model/assumptions';
const nav = [['Assumptions', '/model'], ['Cash Flow', '/model/cashflow'], ['Fiscal Engine', '/model/fiscal-engine'], ['Scenarios', '/model/scenarios'], ['Capital Structure', '/model/capital-structure'], ['Revenue', '/model/revenue'], ['Proponent', '/model/proponent'], ['Benchmark', '/model/benchmark'], ['Monte Carlo', '/model/monte-carlo'], ['Negotiation', '/model/negotiation'], ['Utility', '/model/utility'], ['Audit', '/model/audit'], ['Training', '/model/training']];
export default function ModelNav() { return <aside className="min-h-screen w-full bg-[#003366] p-4 text-white lg:w-72"><h1 className="text-lg font-bold">B.C. Energy Fiscal Model</h1><div className="mt-3 flex gap-2 text-xs"><span className="rounded bg-green-600 px-2 py-1">ACTUAL {ACTUAL_COUNT}</span><span className="rounded bg-red-600 px-2 py-1">FLAG {FLAG_COUNT}</span></div><nav className="mt-6 grid gap-1">{nav.map(([label, href]) => <NavLink key={href} to={href} end={href === '/model'} className={({ isActive }) => `rounded px-3 py-2 text-sm ${isActive ? 'bg-white text-[#003366]' : 'text-blue-50 hover:bg-white/10'}`}>{label}</NavLink>)}</nav></aside>; }
