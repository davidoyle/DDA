import { ReferenceLine, ResponsiveContainer, Scatter, ScatterChart, Tooltip, XAxis, YAxis, CartesianGrid } from 'recharts';
export default function FiscalSpaceDiagram({ points, waccLine }: { points: { royaltyRate: number; projectIRR: number | null; provincialRevenueNPV: number }[]; waccLine: number }) {
  return <div className="h-80 rounded-lg border bg-white p-4"><p className="text-sm font-semibold text-[#003366]">Fiscal space: higher revenue while preserving investability</p><ResponsiveContainer><ScatterChart><CartesianGrid /><XAxis dataKey="provincialRevenueNPV" name="Provincial Revenue NPV" /><YAxis dataKey="projectIRR" name="Project IRR" tickFormatter={(v) => `${(Number(v) * 100).toFixed(0)}%`} /><Tooltip formatter={(v) => typeof v === 'number' ? v.toFixed(2) : v} /><ReferenceLine y={waccLine} stroke="#dc2626" label="WACC" /><Scatter data={points.map((p) => ({ ...p, projectIRR: p.projectIRR ?? 0 }))} fill="#003366" /></ScatterChart></ResponsiveContainer></div>;
}
