import type { ChangeLogEntry } from '@/lib/model/types';
export default function ChangeLogTable({ entries }: { entries: ChangeLogEntry[] }) {
  return <div className="overflow-x-auto rounded-lg border bg-white"><table className="min-w-full text-sm"><thead className="bg-[#003366] text-white"><tr><th className="p-3 text-left">Version</th><th className="p-3 text-left">Date</th><th className="p-3 text-left">Modules</th><th className="p-3 text-left">Description</th><th className="p-3 text-left">Approval</th></tr></thead><tbody>{[...entries].sort((a, b) => b.version - a.version).map((e) => <tr className="border-t" key={`${e.version}-${e.date}`}><td className="p-3">{e.version.toFixed(1)}</td><td className="p-3">{e.date}</td><td className="p-3">{e.modulesAffected.join(', ')}</td><td className="p-3"><strong>{e.description}</strong><br /><span className="text-xs text-slate-500">{e.reason} — {e.sourceOrAuthority}</span></td><td className="p-3"><span className="rounded-full bg-slate-100 px-2 py-1 text-xs">{e.ministryApproval}</span></td></tr>)}</tbody></table></div>;
}
