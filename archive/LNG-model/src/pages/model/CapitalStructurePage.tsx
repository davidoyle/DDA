import { useState } from 'react';
import { BASE_ASSUMPTIONS } from '@/lib/model/assumptions';
import { validateOutput } from '@/lib/model/validation';
import FlagWarningBanner from '@/components/model/FlagWarningBanner';
import { baseFlow, Page, pct, fmt } from './shared';
export default function CapitalStructurePage() { const [debt, setDebt] = useState(0.06); const [equity, setEquity] = useState(0.13); const [ratio, setRatio] = useState(0.45); const { metrics } = baseFlow(); const wacc = debt * ratio + equity * (1 - ratio); return <Page title="Module 5 — Capital Structure"><FlagWarningBanner validation={validateOutput(BASE_ASSUMPTIONS, ['macro.wacc'])}><div className="grid gap-3 md:grid-cols-3">{[['Cost of debt', debt, setDebt], ['Cost of equity', equity, setEquity], ['Debt ratio', ratio, setRatio]].map(([l, v, set]) => <label className="rounded border bg-white p-4" key={String(l)}>{String(l)}<input className="mt-2 w-full" type="number" step="0.01" value={Number(v)} onChange={(e) => (set as (n:number)=>void)(Number(e.target.value))} /></label>)}</div><div className="rounded border bg-white p-4">Derived WACC: <strong>{pct(wacc)}</strong></div><div className="grid gap-3 md:grid-cols-3">{[-0.02,0,0.02].map((d) => <div className="rounded border bg-white p-4" key={d}>WACC {pct(wacc + d)}: {fmt(metrics.npv / (1 + d * 4))}</div>)}</div></FlagWarningBanner></Page>; }
