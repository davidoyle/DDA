import FiscalSpaceDiagram from '@/components/model/FiscalSpaceDiagram';
import { royaltySensitivitySweep } from '@/lib/model/royalty';
import { classifyProject } from '@/lib/model/negotiation';
import { baseFlow, Page, fmt, pct } from './shared';
export default function ProponentPage() { const { metrics } = baseFlow(); const classification = classifyProject(metrics.irr, 0.10); return <Page title="Module 7 — Proponent Returns"><div className="grid gap-3 md:grid-cols-4"><div className="rounded border bg-white p-4">IRR {pct(metrics.irr)}</div><div className="rounded border bg-white p-4">Equity IRR {pct((metrics.irr ?? 0) + 0.03)}</div><div className="rounded border bg-white p-4">NPV {fmt(metrics.npv)}</div><div className="rounded border bg-white p-4">Payback {metrics.paybackYear ?? 'n/a'}</div></div><span className="inline-flex rounded bg-[#003366] px-3 py-1 text-white">{classification}</span><FiscalSpaceDiagram points={royaltySensitivitySweep(metrics.irr, metrics.governmentTakeNPV)} waccLine={0.10} /><div className="grid gap-3 md:grid-cols-3">{[0.08,0.10,0.12].map((w) => <div className="rounded border bg-white p-4" key={w}>WACC {pct(w)}: spread {pct((metrics.irr ?? 0) - w)}</div>)}</div></Page>; }
