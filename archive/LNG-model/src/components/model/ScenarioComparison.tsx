export default function ScenarioComparison({ scenarios }: { scenarios: { label: string; irr: number | null; npv: number; govTake: number; flagsUsed: string[] }[] }) {
  const rows = [{ label: 'IRR', value: (s: typeof scenarios[number]) => s.irr === null ? 'n/a' : `${(s.irr * 100).toFixed(1)}%` }, { label: 'NPV', value: (s: typeof scenarios[number]) => `C${s.npv.toFixed(0)}M` }, { label: 'Gov Take', value: (s: typeof scenarios[number]) => `C${s.govTake.toFixed(0)}M` }, { label: 'Flag count', value: (s: typeof scenarios[number]) => s.flagsUsed.length.toString() }];
  return <div className="overflow-x-auto rounded-lg border bg-white"><table className="min-w-full text-sm"><thead className="bg-[#003366] text-white"><tr><th className="p-3 text-left">Metric</th>{scenarios.map((s) => <th className="p-3 text-left" key={s.label}>{s.label}</th>)}</tr></thead><tbody>{rows.map((r) => <tr className="border-t" key={r.label}><td className="p-3 font-semibold">{r.label}</td>{scenarios.map((s) => <td className="p-3" key={s.label}>{r.value(s)}</td>)}</tr>)}</tbody></table></div>;
}
