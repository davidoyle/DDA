import { useState } from 'react';
import { Bar, BarChart, ResponsiveContainer, XAxis, YAxis, Tooltip } from 'recharts';
import { runMonteCarlo } from '@/lib/model/montecarlo';
import { baseFlow, Page, fmt } from './shared';
export default function MonteCarloPage() { const [iterations, setIterations] = useState(1000); const { metrics } = baseFlow(); const [result, setResult] = useState(() => runMonteCarlo({ iterations, baseNPV: metrics.npv, baseIRR: metrics.irr, baseGovRevenueNPV: metrics.governmentTakeNPV })); const data = result.irrDistribution.map((irr, i) => ({ i, irr })); return <Page title="Module 9 — Monte Carlo"><label className="block rounded border bg-white p-4">Iterations: {iterations}<input className="w-full" type="range" min="100" max="5000" value={iterations} onChange={(e) => setIterations(Number(e.target.value))} /></label><button className="rounded bg-[#003366] px-4 py-2 text-white" onClick={() => setResult(runMonteCarlo({ iterations, baseNPV: metrics.npv, baseIRR: metrics.irr, baseGovRevenueNPV: metrics.governmentTakeNPV }))}>Run</button><div className="grid gap-3 md:grid-cols-4"><div className="rounded border bg-white p-4">Expected NPV {fmt(result.expectedNPV)}</div><div className="rounded border bg-white p-4">P10/P90 {fmt(result.p10NPV)} / {fmt(result.p90NPV)}</div><div className="rounded border bg-white p-4">Negative NPV {(result.probabilityNegativeNPV * 100).toFixed(1)}%</div><div className="rounded border bg-white p-4">Revenue VaR {fmt(result.revenueVaR)}</div></div><div className="h-72 rounded border bg-white p-4"><ResponsiveContainer><BarChart data={data}><XAxis dataKey="i" hide /><YAxis /><Tooltip /><Bar dataKey="irr" fill="#003366" /></BarChart></ResponsiveContainer></div></Page>; }
