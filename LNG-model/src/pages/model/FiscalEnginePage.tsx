import DualOutput from '@/components/model/DualOutput';
import { calculateRoyalty, royaltySensitivitySweep } from '@/lib/model/royalty';
import { BASE_ASSUMPTIONS } from '@/lib/model/assumptions';
import { baseFlow, fmt, pct, Page } from './shared';
export default function FiscalEnginePage() { const { metrics } = baseFlow(); const royalty = calculateRoyalty({ register: BASE_ASSUMPTIONS, spudDate: '2022-09-01', productionYear: 2031, capitalRecovered: true, currentGasPrice: 2.18, grossRevenue: 1000, allowableCosts: 350 }); return <Page title="Module 3 — Fiscal Engine"><div className="rounded border bg-white p-4">Framework: <strong>{royalty.framework}</strong>. Current royalty rate <strong>{pct(royalty.royaltyRate)}</strong>; amount <strong>{fmt(royalty.royaltyAmount)}</strong>.</div><DualOutput rows={royaltySensitivitySweep(metrics.irr, metrics.governmentTakeNPV)} /><div className="rounded border bg-white p-4"><h2 className="font-semibold">OBPS schedule</h2><div className="mt-2 grid grid-cols-5 gap-2">{[2026,2027,2028,2029,2030].map((y) => <div className="rounded bg-slate-50 p-2" key={y}>{y}: C${BASE_ASSUMPTIONS[`tax.obps.${y}`].flagDefault}/t</div>)}</div></div></Page>; }
