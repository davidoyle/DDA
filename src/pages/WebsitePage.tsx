import { useEffect, useMemo, useRef, useState } from 'react';
import type { FormEvent, ReactNode } from 'react';
import { ArrowRight, Check, CircleAlert } from 'lucide-react';
import { Link, useLocation } from 'react-router-dom';
import { pageByRoute, pages, type PublicPage } from '@/content/siteContent';

const fileRoutes: Record<string,string>={
 '01-home.md':'/','02-what-we-do.md':'/what-we-do/','03-fiscal-impact-growth-modelling.md':'/what-we-do/fiscal-impact-growth-modelling/','04-official-community-plan-policy-analysis.md':'/what-we-do/official-community-plan-policy-analysis/','05-economic-development-strategy.md':'/what-we-do/economic-development-strategy/','06-labour-market-analysis.md':'/what-we-do/labour-market-analysis/','07-resource-sector-complex-planning-analysis.md':'/what-we-do/resource-sector-complex-planning-analysis/','08-long-range-financial-scenario-planning.md':'/what-we-do/long-range-financial-scenario-planning/','09-public-interest-research-evidence-packages.md':'/what-we-do/institutional-policy-analysis/','10-who-we-are.md':'/who-we-are/','11-insights.md':'/insights/','12-insight-housing-target-delivery.md':'/insights/when-a-housing-target-outruns-delivery/','13-insight-trade-gap-workforce-number.md':'/insights/the-trade-gap-hidden-inside-a-workforce-number/','14-insight-what-a-flag-tells-you.md':'/insights/when-an-unsupported-number-carries-the-answer/','16-contact.md':'/contact/','17-privacy.md':'/privacy/','18-legal.md':'/legal/','19-terms.md':'/terms/','20-accessibility.md':'/accessibility/'
};
type Block={kind:'p'|'list'|'table';text?:string;items?:string[];rows?:string[][]};
type Subsection={title:string;blocks:Block[]};
type Section={title:string;blocks:Block[];subsections:Subsection[]};
type Document={title:string;intro:Block[];sections:Section[]};
function parse(source:string):Document{const lines=source.trim().split('\n');const doc:Document={title:lines[0].replace(/^# /,''),intro:[],sections:[]};let section:Section|undefined,sub:Subsection|undefined;const target=()=>sub?.blocks??section?.blocks??doc.intro;for(let i=1;i<lines.length;){const line=lines[i].trim();if(!line){i++;continue}if(line.startsWith('## ')){section={title:line.slice(3),blocks:[],subsections:[]};doc.sections.push(section);sub=undefined;i++;continue}if(line.startsWith('### ')){sub={title:line.slice(4),blocks:[]};section?.subsections.push(sub);i++;continue}if(line.startsWith('|')){const rows:string[][]=[];while(i<lines.length&&lines[i].trim().startsWith('|')){const row=lines[i].split('|').slice(1,-1).map(x=>x.trim());if(!/^[-: ]+$/.test(row.join('')))rows.push(row);i++}target().push({kind:'table',rows});continue}if(line.startsWith('- ')){const items:string[]=[];while(i<lines.length&&lines[i].trim().startsWith('- ')){items.push(lines[i].trim().slice(2));i++}target().push({kind:'list',items});continue}let text=line;i++;while(i<lines.length&&lines[i].trim()&&!/^(#{2,3}) |^- |^\|/.test(lines[i].trim())){text+=' '+lines[i].trim();i++}target().push({kind:'p',text})}return doc}
function Rich({text}:{text:string}){const bits:ReactNode[]=[];const re=/(\*\*([^*]+)\*\*|\[([^\]]+)\]\(\.\/([^)]+)\))/g;let last=0,m:RegExpExecArray|null;while((m=re.exec(text))){bits.push(text.slice(last,m.index));bits.push(m[2]?<strong key={m.index}>{m[2]}</strong>:<Link key={m.index} to={fileRoutes[m[4]]??'/'}>{m[3]} <ArrowRight aria-hidden="true"/></Link>);last=re.lastIndex}bits.push(text.slice(last));return <>{bits}</>}
function Blocks({blocks}:{blocks:Block[]}){return <>{blocks.map((b,i)=>b.kind==='p'?<p key={i}><Rich text={b.text!}/></p>:b.kind==='list'?<ul key={i}>{b.items!.map(x=><li key={x}><Rich text={x}/></li>)}</ul>:<div className="public-table" role="region" aria-label="Evidence table" tabIndex={0} key={i}><table><thead><tr>{b.rows![0].map(x=><th scope="col" key={x}><Rich text={x}/></th>)}</tr></thead><tbody>{b.rows!.slice(1).map((row,r)=><tr key={r}>{row.map((x,c)=>c===0?<th scope="row" key={x}><Rich text={x}/></th>:<td key={x}><Rich text={x}/></td>)}</tr>)}</tbody></table></div>)}</>}
function Breadcrumbs({page}:{page:PublicPage}){return <nav className="public-breadcrumb" aria-label="Breadcrumb"><Link to="/">Home</Link><span>/</span>{page.type==='capability-detail'&&<><Link to="/what-we-do/">What we do</Link><span>/</span></>}{page.type==='article'&&<><Link to="/insights/">Insights</Link><span>/</span></>}<span aria-current="page">{page.title}</span></nav>}
function PageHero({doc,kicker,actions=true}:{doc:Document;kicker:string;actions?:boolean}){const opening=doc.intro.length?doc.intro:doc.sections[0]?.blocks??[];return <header className="page-hero"><div className="public-container"><p className="kicker">{kicker}</p><h1>{doc.title}</h1>{!doc.intro.length&&doc.sections[0]&&<h2 className="hero-dek">{doc.sections[0].title}</h2>}<div className="hero-summary"><Blocks blocks={opening}/></div>{actions&&<div className="hero-actions"><Link className="button-primary" to="/what-we-do/">Explore what we do <ArrowRight/></Link><Link className="button-secondary" to="/contact/">Talk to DDA <ArrowRight/></Link></div>}</div></header>}
function ContactBand(){return <section className="contact-band"><div><p className="kicker">Talk to DDA</p><h2>Show us what you are working on.</h2></div><Link className="button-primary" to="/contact/">Talk to DDA <ArrowRight/></Link></section>}
const analysis:Record<string,{label:string;items:string[];statuses?:boolean}>={
 '/what-we-do/fiscal-impact-growth-modelling/':{label:'Scenario architecture',items:['Baseline evidence','Growth assumptions','Infrastructure dependencies','Fiscal consequence','Sensitivity finding']},
 '/what-we-do/official-community-plan-policy-analysis/':{label:'Policy-to-delivery trace',items:['Policy','Servicing','Readiness','Approval mechanism','Delivery']},
 '/what-we-do/economic-development-strategy/':{label:'Opportunity screen',items:['Opportunity','Evidence','Local readiness','Dependencies','Sequence']},
 '/what-we-do/labour-market-analysis/':{label:'Usable-supply decomposition',items:['Occupation','Qualification','Location','Mobility','Timing']},
 '/what-we-do/resource-sector-complex-planning-analysis/':{label:'Critical dependency map',items:['Labour','Infrastructure','Logistics','Approvals','Capital','Schedule']},
 '/what-we-do/long-range-financial-scenario-planning/':{label:'Decision scenario register',items:['Baseline','Alternative scenarios','Sensitivity','Decision triggers','Review point']},
 '/what-we-do/institutional-policy-analysis/':{label:'Claim-and-source register',items:['ACTUAL: named and dated','PROXY: informative comparison','FLAG: material unresolved gap','Claim affected','Resolution path'],statuses:true}
};
function AnalysisModule({page}:{page:PublicPage}){const item=analysis[page.route];return <figure className={`analysis-module analysis-${page.id.toLowerCase()}`}><figcaption><span>Decision instrument</span><strong>{item.label}</strong></figcaption><ol>{item.items.map((x,i)=><li key={x} className={item.statuses?`status-${i<3?['actual','proxy','flag'][i]:'neutral'}`:''}><span>{String(i+1).padStart(2,'0')}</span><b>{x}</b>{i<item.items.length-1&&<ArrowRight aria-hidden="true"/>}</li>)}</ol><p><Check aria-hidden="true"/> Every input remains connected to the decision it can change.</p></figure>}
const capabilities=[['Learn the system','Trace the organizations, policy, infrastructure, labour, economics, and timing that belong to the problem.'],['Reconstruct the evidence','Connect fragmented sources, definitions, dates, datasets, and claims.'],['Test what matters','Find the constraint, gap, dependency, assumption, or exposure and establish what it changes.'],['Build the response','Produce the analysis or working asset the situation requires.']];
function HomeTemplate({doc}:{doc:Document}){const insights=doc.sections.find(x=>x.title==='Evidence of how DDA thinks')!,mandate=doc.sections.find(x=>x.title==='Already holding the mandate?')!,contact=doc.sections.find(x=>x.title==='Show us what you are working on')!;return <><PageHero doc={doc} kicker="Investigation · Evidence · Analysis"/><section className="method-rail" aria-labelledby="capabilities-heading"><div className="public-container"><h2 id="capabilities-heading">What the work does</h2>{capabilities.map(([title,description],i)=><div key={title}><span>0{i+1}</span><strong>{title}</strong><p>{description}</p></div>)}</div></section><section className="editorial-section public-container"><div className="section-heading"><p className="kicker">Inside the evidence</p><h2>{insights.title}</h2></div><div className="insight-layout">{insights.subsections.map((section,i)=><article className={i===0?'insight-lead':'insight-card'} key={section.title}><span>0{i+1} / Insight</span><h3>{section.title}</h3><Blocks blocks={section.blocks}/></article>)}</div></section><section className="work-home"><div className="public-container"><div className="section-heading"><p className="kicker">For consulting teams</p><h2>{mandate.title}</h2></div><div className="mandate-copy"><Blocks blocks={mandate.blocks}/></div></div></section><section className="home-close public-container"><div><p className="kicker">Talk to DDA</p><h2>{contact.title}</h2><Blocks blocks={contact.blocks}/></div><Link className="button-primary" to="/contact/">Talk to DDA <ArrowRight/></Link></section></>}
function HubTemplate({doc}:{doc:Document}){const approach=doc.sections.find(x=>x.title==='How DDA gets into a problem')!,apps=doc.sections.find(x=>x.title==='Areas where we commonly work')!;return <><PageHero doc={doc} kicker="What we do" actions={false}/><main className="public-container hub-content"><section><div className="section-heading"><p className="kicker">How the work develops</p><h2>{approach.title}</h2></div><div className="method-grid">{approach.subsections.map((section,i)=><article key={section.title}><span>0{i+1}</span><h3>{section.title}</h3><Blocks blocks={section.blocks}/></article>)}</div></section><section><div className="section-heading"><p className="kicker">Common areas of work</p><h2>{apps.title}</h2></div><Blocks blocks={apps.blocks}/><div className="application-grid">{apps.subsections.map(section=><article key={section.title}><h3>{section.title}</h3><Blocks blocks={section.blocks}/></article>)}</div></section>{doc.sections.filter(x=>x!==approach&&x!==apps).map(section=><section className="standard-section" key={section.title}><h2>{section.title}</h2><Blocks blocks={section.blocks}/></section>)}</main><ContactBand/></>}
function sectionId(title:string){return title.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')}
function DetailTemplate({doc,page}:{doc:Document;page:PublicPage}){const sections=doc.intro.length?doc.sections:doc.sections.slice(1);return <><Breadcrumbs page={page}/><PageHero doc={doc} kicker="What we do" actions={false}/><div className="detail-layout public-container"><aside className="on-page"><strong>On this page</strong>{sections.slice(0,5).map(x=><a key={x.title} href={`#${sectionId(x.title)}`}>{x.title}</a>)}</aside><main>{sections.map((s,i)=><section id={sectionId(s.title)} className={i===3?'analysis-section':'detail-section'} key={s.title}><p className="section-number">0{i+1}</p><h2>{s.title}</h2><Blocks blocks={s.blocks}/>{i===3&&<AnalysisModule page={page}/>}</section>)}</main></div><ContactBand/></>}
function InsightsHubTemplate({doc}:{doc:Document}){const featured=doc.sections.find(x=>x.title==='Featured')!,more=doc.sections.find(x=>x.title.includes('More'))!;return <><PageHero doc={doc} kicker="Insights" actions={false}/><main className="public-container insights-hub"><section className="featured-insight">{featured.subsections.map(s=><article key={s.title}><p className="kicker">Featured perspective</p><h2>{s.title}</h2><Blocks blocks={s.blocks}/></article>)}</section><section className="supporting-insights">{more.subsections.map(s=><article key={s.title}><p className="kicker">Perspective</p><h2>{s.title}</h2><Blocks blocks={s.blocks}/></article>)}</section>{doc.sections.slice(1).filter(x=>x!==featured&&x!==more).map(s=><section className="standard-section" key={s.title}><h2>{s.title}</h2><Blocks blocks={s.blocks}/>{s.subsections.map(x=><div key={x.title}><h3>{x.title}</h3><Blocks blocks={x.blocks}/></div>)}</section>)}</main><ContactBand/></>}
function ArticleTemplate({doc,page}:{doc:Document;page:PublicPage}){const intro=doc.intro.filter(block=>block.kind!=='p'||!block.text?.startsWith('**Perspective'));return <><Breadcrumbs page={page}/><article className="article-shell"><header><div className="article-meta"><span>Perspective</span><span>{page.topics[0]}</span><span>{page.readTime}</span></div><h1>{doc.title}</h1><Blocks blocks={intro}/></header>{doc.sections.map(s=><section key={s.title} className={s.blocks.some(x=>x.kind==='table')?'article-evidence':''}><h2>{s.title}</h2><Blocks blocks={s.blocks}/>{s.subsections.map(x=><div key={x.title}><h3>{x.title}</h3><Blocks blocks={x.blocks}/></div>)}</section>)}</article><ContactBand/></>}
function AboutTemplate({doc}:{doc:Document}){const sections=doc.intro.length?doc.sections:doc.sections.slice(1);return <><PageHero doc={doc} kicker="Who we are" actions={false}/><main className="about-layout public-container">{sections.map((s,i)=><section key={s.title} className={i===0?'about-lead':''}><span>0{i+1}</span><h2>{s.title}</h2><Blocks blocks={s.blocks}/>{s.subsections.length>0&&<div className="method-grid">{s.subsections.map(x=><article key={x.title}><h3>{x.title}</h3><Blocks blocks={x.blocks}/></article>)}</div>}</section>)}</main><ContactBand/></>}
function ContactForm(){const [state,setState]=useState<'idle'|'sending'|'success'|'error'>('idle'),[errors,setErrors]=useState<Record<string,string>>({});const summary=useRef<HTMLDivElement>(null);const fields=[['name','Name','text',true],['organization','Organization, optional','text',false],['email','Email','email',true],['message','What are you working on?','textarea',true]] as const;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(state==='sending')return;const form=new FormData(e.currentTarget),next:Record<string,string>={};for(const [id,label,,required] of fields){const value=String(form.get(id)??'').trim();if(required&&!value)next[id]=`${label} is required.`;if(id==='email'&&value&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value))next[id]='Enter a valid email address.'}if(Object.keys(next).length){setErrors(next);setTimeout(()=>summary.current?.focus());return}setErrors({});setState('sending');try{const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});if(!response.ok)throw new Error();setState('success')}catch{setState('error')}}return <section className="intake"><div className="intake-intro"><p className="kicker">Start here</p><h2>Start with what you are working on.</h2><p>A few sentences are enough. Tell us what you are trying to solve, what changed, or what you have been asked to deliver. You can also paste a public link to the project, RFP, report, dataset, or issue.</p><div className="confidentiality"><CircleAlert/><p>Do not send privileged, personal, or commercially confidential information in an unsecured first message. A suitable transfer method can be arranged.</p></div></div><div>{Object.keys(errors).length>0&&<div className="error-summary" role="alert" tabIndex={-1} ref={summary}><strong>Check the following fields</strong><ul>{Object.entries(errors).map(([id,x])=><li key={id}><a href={`#${id}`}>{x}</a></li>)}</ul></div>}{state==='success'?<div className="form-success" role="status"><Check/><h2>Inquiry sent.</h2><p>DDA will review the problem and get in touch to start a conversation where there is a fit.</p></div>:<form onSubmit={submit} noValidate>{fields.map(([id,label,type,required])=><div className="form-field" key={id}><label htmlFor={id}>{label} {required&&<span>Required</span>}</label>{type==='textarea'?<textarea id={id} name={id} aria-invalid={!!errors[id]} aria-describedby={errors[id]?`${id}-error`:undefined}/>:<input id={id} name={id} type={type} aria-invalid={!!errors[id]} aria-describedby={errors[id]?`${id}-error`:undefined}/>} {errors[id]&&<small id={`${id}-error`}>{errors[id]}</small>}</div>)}<button className="button-primary" disabled={state==='sending'}>{state==='sending'?'Sending…':'Send inquiry'} <ArrowRight/></button>{state==='error'&&<p className="form-failure" role="alert">The inquiry could not be sent. Your entries remain in place; please try again.</p>}</form>}<p className="disclaimer">Sending an inquiry does not create a client relationship or engagement. Work begins only under agreed written terms.</p></div></section>}
function ContactTemplate({doc,page}:{doc:Document;page:PublicPage}){return <><Breadcrumbs page={page}/><PageHero doc={doc} kicker="Contact" actions={false}/><main className="public-container"><ContactForm/></main></>}
function UtilityTemplate({doc,page}:{doc:Document;page:PublicPage}){return <><Breadcrumbs page={page}/><article className="utility-shell"><header><p className="kicker">Information</p><h1>{doc.title}</h1><Blocks blocks={doc.intro}/></header>{doc.sections.map(s=><section key={s.title}><h2>{s.title}</h2><Blocks blocks={s.blocks}/>{s.subsections.map(x=><div key={x.title}><h3>{x.title}</h3><Blocks blocks={x.blocks}/></div>)}</section>)}</article></>}
export default function WebsitePage(){const {pathname}=useLocation(),key=pathname==='/'?'/':pathname.endsWith('/')?pathname:`${pathname}/`,page=pageByRoute[key],doc=useMemo(()=>parse(pages[key]),[key]);useEffect(()=>{document.documentElement.dataset.publicPage=page.type;return()=>{delete document.documentElement.dataset.publicPage}},[page.type]);switch(page.type){case'home':return <HomeTemplate doc={doc}/>;case'capability-hub':return <HubTemplate doc={doc}/>;case'capability-detail':return <DetailTemplate doc={doc} page={page}/>;case'about':return <AboutTemplate doc={doc}/>;case'insights-hub':return <InsightsHubTemplate doc={doc}/>;case'article':return <ArticleTemplate doc={doc} page={page}/>;case'contact':return <ContactTemplate doc={doc} page={page}/>;default:return <UtilityTemplate doc={doc} page={page}/>}}
